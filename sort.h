#ifndef SORT_H
#define SORT_H

#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

void bubble_sort(int *array, size_t size);
void swap(int *a, int *b);
void print_array(int arr[], int size);

#endif
